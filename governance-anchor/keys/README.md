# Program keypair (gitignored)

Place `owltopia_governance-keypair.json` here locally. It is **never** committed (`keys/*-keypair.json` is in `.gitignore`).

Generate or copy per `.env.example` / Anchor deploy docs, then run `npm run copy:governance-keypair` from the repo root if your workflow copies into `target/deploy/`.
