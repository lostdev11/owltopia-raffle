'use client'

import { Footer } from '@/components/Footer'

/** Renders the footer on all pages so links (including glass icon card) are always visible. */
export function ConditionalFooter() {
  return <Footer />
}
