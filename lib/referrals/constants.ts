/** Client-readable cookie: landing ?ref= is copied here for checkout attribution. */
export const REFERRAL_COOKIE_NAME = 'owl_ref'

/** Max-age 30 days (seconds). */
export const REFERRAL_COOKIE_MAX_AGE_SEC = 60 * 60 * 24 * 30
