/** Hard cap for one batch checkout request/verification cycle (single on-chain tx). */
export const CART_BATCH_MAX_RAFFLES_PER_TX = 20

/** Hard cap for total raffles handled by one checkout click (processed as chunked batches). */
export const CART_CHECKOUT_MAX_RAFFLES = 200
